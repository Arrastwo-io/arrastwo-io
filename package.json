{
  "name": "@Arrastwo-io/arrastwo-io",
  "version": "1.0.0",
  "main": "server0/index.js",
  "scripts": {
    "test": "exit 0",
    "start": "concurrently \"node --optimize-for-size --no-lazy --gc_interval=120 server0/index\" \"node --optimize-for-size --no-lazy --gc_interval=120 server1/index\""
  },
  "dependencies": {
    "concurrently": "^8.2.0",
    "node-fetch": "^3.3.2",
    "ws": "^8.13.0"
  },
  "engines": {
    "node": "16.x"
  },
  "publishConfig": {
    "@Arrastwo-io:arrastwo-io": "https://npm.pkg.github.com"
  },
  "license": "MIT"
}
